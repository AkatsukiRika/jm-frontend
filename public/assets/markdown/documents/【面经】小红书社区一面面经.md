## 面试形式
在线视频面试，一面无算法题。  
面试时间：2023/07/03 15:00~16:00

## 面试流程

### 1. 自我介绍

### 2. 介绍在字节做过最有技术深度的一件事 —— Flutter Web 移植工程

- 移植是如何实现的？移植后的 Flutter 项目架构怎样？

- Flutter Web 项目在哪些地方会存在性能瓶颈？  
答：JavaScript 层和 C++ 层的数据交换存在瓶颈，目前采用的是 JSON 格式，数据量大了以后 encode / decode 的过程会有瓶颈。

- 像这种跨端开发，数据在内存中是怎样实现共享的，有了解过吗？  
答：简单了解过一点，举例 WebAssembly 中 UTF String 类型的数据交换。

### 3. Android UI 相关的基础问题

有一个 Button，包裹在 ScrollView 里面。手指放在 Button 上，拖动几个像素后抬起，这个过程中的触摸事件是如何传递的？

### 4. 线程 / 协程相关问题

- Kotlin 协程的概念，协程在 JVM 上是如何实现的？它跟线程比的优势在哪？

- 你之前的项目里，是如何实现线程同步的？  
答：一般使用 synchronized 方法和 volatile 关键字。

- volatile 一般用在什么场景？如果要控制一个 int 类型变量的访问，需要使用 volatile 关键字吗？它和 synchronized 的区别在哪，是否是等价的？

### 5. 内存泄露相关问题

- 简历上做过内存防劣化的项目，在这个项目中扮演的是什么样的角色？

- 防劣化的这个工具是怎么做的呢，有了解过吗？

- Java 中四种引用的区别？它们的使用场景是什么样的？