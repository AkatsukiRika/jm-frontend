## 面试形式
在线视频面试，一面无算法题。  
面试时间：2023/07/10 14:30~15:10

## 面试流程

### 1. 自我介绍

### 2. 你做的项目是图像相关的，说说有关 Bitmap 都了解什么？

- 用户导入进来的 Bitmap 有没有经过复用？用了什么样的复用策略？

- 如果导入一张 10000 * 10000 的 Bitmap，如何处理？

### 3. 你提到高端机可以支持 4K 甚至 5K 图，会导致 OOM 吗？

- 如何跟踪 OOM？跟踪到 OOM 之后，怎么去分析和处理？

- 你有解决过内存泄露的问题吗？是怎样解决的？

- 内存泄露的 SDK 是怎么跟踪 GCRoot 的？

- 使用 OKHttp 开启一个连接，Activity 被销毁之后，会有内存泄露吗？这时候的 GCRoot 链路是怎样的？

### 4. 讲讲 Android 手势触摸事件的处理

- ViewGroup 嵌套一个 View，手指从 View 上开始滑动，一直滑动到 ViewGroup 外部。打印事件，会打印出什么结果？

- 你知道 ACTION_CANCEL 吗？在上面的过程中，是 View 还是 ViewGroup 会接收到 ACTION_CANCEL 呢？

- 如何处理嵌套滑动和手势冲突？除了 GestureDetector 还有什么办法？

### 5. 讲讲 LiveData 的一些基本概念

- LiveData 是怎么实现的？它本身有几种状态？

- LiveData 绑定的 LifecycleOwner 在 Activity 的哪个子类里面？直接继承 Activity，能用到 LifecycleOwner 吗？

- 在自定义 View 里面应该怎么使用 LiveData，该绑定谁的 LifecycleOwner？

### 6. 说一个自己解决过比较难的 Bug 或比较难的事情 —— Flutter Web 移植

- 这件事中间的难点是什么？如何解决？  
答：JS 和 C++ 层的数据传递，使用 JSON，如果优化，应该会采用 Protobuf.

- Protobuf 相比 JSON 有什么优点？

- Protobuf 相比 JSON，在哪些方面能够节省空间？

- Protobuf 是如何将数据结构转换成二进制数据流的？

### 7. 说说 RecyclerView 相关的一些知识点

- RecyclerView 有几级缓存？分别对应什么？

- RecyclerView 的第四级缓存里，是以什么为依据来缓存的？

### 8. 介绍一下适配器模式？